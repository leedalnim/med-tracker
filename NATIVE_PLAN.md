# 복약 트래커 — 아이폰 설치용(네이티브) 전환 계획

> 지금은 **웹 PWA**(HTML/CSS/JS, GitHub Pages 배포 + Supabase 클라우드 백업).
> 이 문서는 **맥북 + 아이폰, 혼자 쓸 용도**로 앱을 직접 빌드해 설치할 때의 계획이에요. **계획용 문서 — 아직 실행 X.**

---

## 0. 왜 네이티브? (우리 앱 기준 실익)

| 목적 | 웹(PWA)으로 충분? | 비고 |
|---|---|---|
| 복약 간격 계산·기록 | ✅ 이미 됨 | 코어 기능 |
| 데이터 백업/복원 | ✅ 이미 됨 | 파일 내보내기 + **클라우드(복구 코드)** |
| **복약 알림(리마인더)** | ❌ | 네이티브의 1순위 이유 |
| 데이터 영속성(안 지워짐) | 위험 있음 | 네이티브 웹뷰는 OS가 저장소 안 지움 |
| **위젯 / 잠금화면 / Live Activity** | ❌ | 네이티브 전용 |
| Apple Health·애플워치 | ❌ | 유료 계정 필요(HealthKit) |
| Face ID 잠금 | ❌ | 민감 정보 보호 |

**결론**: 네이티브로 갈 진짜 이유는 **알림 · 위젯 · 데이터 영속성**. 백업은 이미 해결됨.

### iOS 웹 알림이 안 되는 이유
아이폰 사파리는 **Web Push만** 지원하고(그것도 홈화면 추가 필수, 서버 필요),
**시간 예약형 로컬 알림 API 자체가 없음**. 즉 "3시간 뒤 알려줘"를 웹 코드로는 못 만듦.
→ 알림을 원하면 네이티브 껍데기가 반드시 필요.

---

## 1. 두 가지 길

### A. Capacitor로 지금 웹앱 감싸기 (권장 1단계 · 저비용)
- 지금 웹 코드(index.html·app.js·style.css)를 **그대로 재사용**, 네이티브 껍데기만 씌움.
- 알림은 플러그인 한 줄로 해결. iOS·Android 한 코드로 둘 다 나옴.
- 단점: 위젯·Live Activity는 **추가 네이티브(Swift) 작업** 필요.

### B. SwiftUI 풀 네이티브 (iOS 전용, 재작성)
- Swift로 처음부터 재작성. 위젯·Live Activity·리퀴드 글라스 전부 가능.
- 단점: JS 로직(간격 계산·마이그레이션·클라우드 동기화)을 Swift로 **전부 재작성**.

**추천: A로 시작.** 지금 앱이 이미 완성도 높게 굴러가고 있어서, 재작성은 손해가 큼.

---

## 2. 필요한 것 (Capacitor 기준)

### 개발 환경 (맥북에 설치)
- [ ] **Node.js + npm** (`node -v`로 확인)
- [ ] **Xcode** (App Store, 무료 · 용량 큼)
- [ ] **CocoaPods** (`sudo gem install cocoapods` 또는 `brew install cocoapods`)
- [ ] Capacitor (`npm i @capacitor/core @capacitor/cli`)

### 계정 / 비용 — 혼자 쓸 때 핵심 갈림길

| | 무료 Apple ID | Apple Developer Program |
|---|---|---|
| 비용 | 0원 | **$99 / 년** |
| 서명 유효기간 | **7일** (지나면 앱이 안 열림 → 맥북 연결해 재설치) | **1년** |
| 등록 가능 앱 수 | 동시 3개 | 사실상 무제한 |
| 로컬 알림 | ✅ 됨 | ✅ 됨 |
| 위젯(App Group) | ⚠️ 제약 있음 | ✅ 안정적 |
| TestFlight·무선 설치 | ❌ | ✅ |

> 알림만 원하면 **무료로 충분** (단 7일마다 재설치).
> **위젯까지 제대로 원하면 $99 유료 계정을 권장.**

---

## 3. 실행 단계 개요 (1단계 — 알림까지)

1. Node·Xcode·CocoaPods 설치 확인
2. 저장소에 `capacitor.config.json` 추가 → **webDir를 현재 웹 자산 폴더로 지정**
3. `npx cap add ios` → `ios/` 네이티브 프로젝트 생성
4. `npm i @capacitor/local-notifications` → `npx cap sync ios`
5. 앱 안에 **"이 약 알림 켜기" 설정 UI** 추가 (약별 시간/간격 기반 예약)
6. Xcode에서 아이폰 연결 → **내 Apple ID로 서명 → 실기기 설치**
7. 폰에서 **설정 > 클라우드 백업 > 복구 코드 입력** → 기존 기록 전부 복원

### ⭐ 기존 기록은 안전
새로 설치한 앱은 저장소가 **빈 칸에서 시작**하지만, 지금 클라우드 백업이 켜져 있어서
**복구 코드만 입력하면 약·복용기록·생리·부정출혈이 전부 그대로 들어옴.**
(웹 PWA도 그대로 두고 병행 사용 가능 — 같은 코드를 쓰면 양쪽이 동기화됨)

---

## 4. 우리 앱 특화 체크포인트
- **상대경로 사용 중** → Capacitor 환경에서도 잘 동작 (유리)
- **데이터 영속성** ✅ — 네이티브 웹뷰 localStorage는 OS가 안 지움
- **서비스워커** — 네이티브에선 앱 번들이 대신하므로 불필요(둬도 무해)
- **클라우드 백업** — Capacitor 웹뷰에서도 `fetch` 그대로 동작 (Supabase 그대로 사용)
- **앱 아이콘·스플래시** — 지금 `icons/` 있음 → 네이티브용 사이즈 세트 추가 필요

---

## 5. 알림 설계 (구현할 때)
- `@capacitor/local-notifications`로 **복용 예정 시각에 로컬 알림 예약**
- 예약 시점: "먹었어요"를 누른 순간 → `다음 복용 시각`에 알림 하나 예약
- 취소: 기록 삭제·약 삭제·간격 변경 시 예약 취소 후 재예약
- **iOS 제약**: 앱당 **대기 중 알림 64개 제한** → 약별 1~2개만 굴리면 충분
- **소리 무시하는 긴급 알림(Critical Alert)** 은 애플 별도 승인 필요 → 일반 알림으로 진행

---

## 6. 위젯 (2단계)

### 중요: Capacitor "그 자체"로는 위젯 안 나옴
위젯은 웹뷰 밖에서 도는 **완전 네이티브 부품**이라 웹 코드 재사용 불가.
- **iOS**: WidgetKit + **SwiftUI(Swift)**

### 그래도 붙일 수 있음
1. `ios/` 프로젝트에 **위젯 익스텐션 타겟 추가**
2. 위젯 UI를 **SwiftUI로 작성** (예: "비잔정 · 다음 21:00 · 3시간 후")
3. **데이터 공유 브릿지** — 웹앱이 계산한 "다음 복용 시각/오늘 카운트"를
   **App Group(공유 UserDefaults)** 로 넘겨 위젯이 읽게 (작은 커스텀 플러그인 필요)

### 제약
- 위젯은 **초단위 실시간 갱신 불가** → 타임라인(주기) 갱신.
  대신 SwiftUI `Text(date, style: .timer)` 로 남은 시간은 자동 카운트다운 가능.
- 초단위 링 애니메이션 = **Live Activity(다이나믹 아일랜드)**, 난이도 더 높음.
- **App Group**은 유료 계정에서 안정적.

| 원하는 것 | 웹 재사용 | 난이도 |
|---|---|---|
| 복약 알림 | ✅ | 쉬움 (`local-notifications`) |
| 위젯(다음 복용 표시) | ❌ | 중간 (위젯 타겟 + 브릿지) |
| Live Activity/다이나믹 아일랜드 | ❌ | 어려움 |

---

## 7. 추천 진행 순서
1. **Capacitor + 알림** 먼저 — 웹 코드 그대로, 무료 계정으로도 가능
2. 실제로 잘 쓰이면 → **$99 결제 후 위젯 익스텐션** 추가
3. 위젯·Live Activity가 핵심 경험이 되면 → SwiftUI 풀 네이티브 재작성 검토

---

## 8. 시작 전 미리 확인해둘 것
- [ ] `node -v` 되는지
- [ ] Xcode 설치 여부 / 버전
- [ ] 무료 Apple ID로 갈지 / $99 유료로 갈지 결정
- [ ] **클라우드 백업 복구 코드 안전한 곳에 저장해뒀는지** (네이티브 앱 첫 실행 때 필요)

---

## 9. 이 저장소에서 준비해줄 수 있는 것
빌드는 맥북에서 직접 해야 하지만(이 환경엔 Xcode가 없음), 아래는 미리 만들어 커밋해둘 수 있어요.
- `capacitor.config.json` + `package.json`
- 알림 설정 UI + 예약/취소 로직 (`app.js`) — **웹에서는 자동으로 비활성**, 네이티브에서만 켜짐
- 맥북에서 그대로 복붙할 **터미널 명령 순서 문서**
- 위젯 단계용 App Group 브릿지 플러그인 뼈대
