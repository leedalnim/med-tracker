# 복약 트래커 — 네이티브 앱 전환 계획

> 지금은 **웹 PWA**(HTML/CSS/JS, GitHub Pages 배포). 이 문서는 나중에 네이티브로 갈 때를 위한 계획·체크리스트예요. **당장 실행 X, 계획용.**

---

## 0. 왜 네이티브? (우리 앱 기준 실익)

| 목적 | 웹(PWA)으로 충분? | 비고 |
|---|---|---|
| 복약 간격 계산·기록 | ✅ 이미 됨 | 코어 기능 |
| 데이터 백업/복원 | ✅ 이미 됨 | 설정 > 데이터 백업 (내보내기/불러오기) |
| **복약 알림(리마인더)** | ❌ | 네이티브의 1순위 이유 |
| 데이터 영속성(안 지워짐) | 위험 있음 | 네이티브 웹뷰는 OS가 저장소 안 지움 |
| **위젯 / 잠금화면 / Live Activity** | ❌ | 네이티브 전용 |
| Apple Health·애플워치 | ❌ | 유료 계정 필요(HealthKit) |
| Face ID 잠금 | ❌ | 민감 정보 보호 |
| 리퀴드 글라스(.glassEffect) | ❌ | SwiftUI 네이티브 전용 |

**결론**: 네이티브로 갈 진짜 이유는 **알림 · 데이터 영속성 · 위젯**. 백업은 이미 웹으로 해결됨.

---

## 1. 두 가지 길

### A. Capacitor로 지금 웹앱 감싸기 (권장 1단계 · 저비용)
- 지금 웹 코드(index.html·app.js·style.css)를 **그대로 재사용**, 네이티브 껍데기만 씌움.
- iOS·Android **한 코드로 둘 다** 나옴.
- 단점: 위젯·Live Activity·리퀴드 글라스 같은 순정 네이티브 효과는 **추가 네이티브 작업** 필요.

### B. SwiftUI 풀 네이티브 (iOS 전용, 재작성)
- Swift로 처음부터 재작성. 위젯·Live Activity·리퀴드 글라스 전부 가능.
- 단점: JS 로직(간격 계산·마이그레이션)을 Swift로 **재작성**, 안드로이드는 또 별도(Kotlin).

---

## 2. 필요한 것 (Capacitor 기준)

### 개발 환경 (맥에 설치)
- [ ] **Node.js + npm** (`node -v`로 확인)
- [ ] **Capacitor** (`@capacitor/core`, `@capacitor/cli`)
- [ ] **iOS**: Xcode(보유) + **CocoaPods**
- [ ] **Android**(원하면): **Android Studio** (JDK·SDK 포함)

### 계정 / 비용
- **iOS · 내 폰만**: 무료 Apple ID(7일마다 재설치) / 또는 Apple Developer Program **$99·년**(1년 서명, TestFlight, 푸시·HealthKit)
- **Android**: **무료** — APK 만들어 그냥 설치(스토어·계정 불필요, 만료 없음)

---

## 3. 실행 단계 개요 (나중에 할 순서)

1. Node·Capacitor 설치
2. `capacitor.config` 생성 → **webDir를 현재 웹 자산 폴더로 지정**
3. `npx cap add ios` / `npx cap add android` → 네이티브 프로젝트 폴더 생성
4. Xcode / Android Studio에서 열어 **실기기 빌드·설치**
5. (원하면) 알림 플러그인 추가
6. **앱 아이콘·스플래시** 네이티브용 사이즈 생성 (지금 `icons/` 있음 → 세트 추가 필요)

---

## 4. 우리 앱 특화 체크포인트
- **상대경로 사용 중** → Capacitor 환경에서도 잘 동작 (유리)
- **데이터 영속성** ✅ — 네이티브 웹뷰 localStorage는 OS가 안 지움 (PWA 7일 문제 해결)
- **서비스워커** — 네이티브에선 오프라인이 앱 번들로 대체돼 굳이 불필요(둬도 무해)
- **기존 기록 이전** — 웹에서 **내보내기(복약백업.json)** → 네이티브 앱에서 **불러오기**로 그대로 이사 (새 앱은 저장소가 빈 칸에서 시작하므로 이 과정 필요)
- 백업 더 매끄럽게: `@capacitor/share`·`@capacitor/filesystem` (선택)

---

## 5. 알림 (핵심 목적이면)
- [ ] **`@capacitor/local-notifications`** 플러그인 추가 → 복약 시간 예약 알림
- iOS **무료 계정으로도 로컬 알림 동작** (푸시 서버 불필요)
- 앱에 **알림 권한 요청 + "이 약 알림 켜기" 설정 UI** 추가 작업

---

## 6. 위젯 / Live Activity

### 중요: Capacitor "그 자체"로는 위젯 안 나옴
위젯은 웹뷰 밖에서 도는 **완전 네이티브 부품**이라 웹 코드 재사용 불가.
- **iOS**: WidgetKit + **SwiftUI(Swift)**
- **Android**: App Widget + **Kotlin**

### 그래도 Capacitor + 네이티브 위젯 확장으로 가능
1. `ios/`·`android/` 프로젝트에 **위젯 타겟 수동 추가**
2. 위젯 UI **네이티브로 작성**
3. **데이터 공유 브릿지** — 웹앱이 계산한 "다음 복용 시각/오늘 카운트"를 **App Group(공유 저장소)**로 넘겨 위젯이 읽게 (작은 커스텀 플러그인 필요)

### 제약
- 위젯은 **실시간 초단위 갱신 불가** → **타임라인(주기) 갱신**. "다음 복용 15:30" / "3시간 후" 식으로 표시.
- 초단위 카운트다운 느낌은 **Live Activity(다이나믹 아일랜드)** — 역시 네이티브 전용, 난이도 더 높음.
- **App Group** 설정은 **무료 계정에서 제약** 있을 수 있음(유료 계정 권장).

| 원하는 것 | 웹 재사용 | 추가 작업 난이도 |
|---|---|---|
| 복약 알림 | ✅ | 쉬움 (`local-notifications`) |
| 위젯(다음 복용 표시) | ❌ | 중간~어려움 (위젯 타겟 + 브릿지) |
| Live Activity/다이나믹 아일랜드 | ❌ | 어려움 |

---

## 7. 추천 진행 순서
1. **Capacitor + 알림** 먼저 (가성비, 웹 그대로 재사용, 데이터 영속성 확보)
2. 위젯이 진짜 필요해지면 → **iOS 네이티브 위젯 확장** 하나 붙이기
3. 위젯·Live Activity가 핵심 경험이 되면 → **SwiftUI 풀 네이티브** 재작성 검토

---

## 8. 시작 전 미리 확인해둘 것
- [ ] `node -v` 되는지
- [ ] Xcode 버전 (최신 Capacitor는 비교적 최근 macOS/Xcode 필요)
- [ ] (안드로이드도 할 거면) Android Studio 설치 여부
- [ ] 무료 Apple ID로 갈지 / $99 유료로 갈지 결정
