# 인수인계 — 여기까지 했고, 다음은 이것

> 새 세션(특히 **맥북의 Claude Code**)이 바로 이어서 작업할 수 있도록 남기는 문서.
> 마지막 갱신: 2026-09-03 · 앱 버전 v112

---

## 1. 지금 어떤 상태인가

**웹 PWA는 사실상 완성.** 아이폰 홈 화면에 추가해서 매일 쓰고 계세요.

최근에 붙인 것 (v100 → v112):

| | 상태 |
|---|---|
| 클라우드 백업 (Supabase) | ✅ 동작 중. 복구 코드로 다른 폰에서 복원 가능 |
| 복약 알림 (Web Push) | ✅ **실제로 폰에 도착 확인함** |
| 알림 시각 설정 (간격 기준 / 매일 시각) | ✅ |
| 약 관리 레이어 팝업 | ✅ 설정에서 세 덩어리를 팝업 하나로 |
| 알림 누르면 그 약 화면으로 | ✅ v112 |

서버 쪽(Supabase)도 전부 설정 완료:
`med_backup`, `med_push`, `med_alarm` 테이블 + `med-push` Edge Function + pg_cron 매분 실행.
`cron.job_run_details`에서 `succeeded`가 1분 간격으로 쌓이는 것 확인함.

---

## 2. 남은 것 — 왜 네이티브로 가려는가

사용자가 원하는 건 **"알람"** 인데, 지금 만든 건 **"푸시 알림"** 이에요. 이건 다른 물건입니다.

| | 지금 (웹 푸시) | 원하는 것 (알람) |
|---|---|---|
| 무음/진동 모드 | 진동만 | **무조건 울림** |
| 화면 | 배너 하나 | **전체 화면 점유** |
| 끌 때까지 | 그냥 사라짐 | **계속 울림** |

**웹으로는 여기가 천장이에요.** iOS가 웹앱에 그 권한을 주지 않아요.
지금은 **시계 앱 알람으로 깨우고 → 푸시 알림을 눌러 앱으로 들어가는** 식으로 병행 중.

### 그래서 네이티브 (AlarmKit)

iOS 26부터 **AlarmKit**이 열려서, 일반 앱도 시계 앱처럼 무음을 뚫는 알람을 만들 수 있어요.
사용자는 **iOS 26.6.1**, **맥북 보유**, **혼자 쓸 용도**입니다.

---

## 3. 확인해둔 사실 (다시 조사하지 말 것)

- **AlarmKit은 별도 entitlement가 필요 없어요.**
  `Info.plist`의 `NSAlarmKitUsageDescription` + 런타임 권한 요청이 전부.
  인터넷에 "애플 승인이 필요하다"는 말이 도는데, **애플 엔지니어가 포럼에서
  "그건 LLM이 지어낸 존재하지 않는 entitlement"라고 직접 정정**했어요.
  → https://developer.apple.com/forums/thread/797950
  → 따라서 **무료 Apple ID로도 될 가능성이 높음.** 단, 실제 빌드로 확인 필요. ← 이게 첫 관문
- **위젯은 무료 계정에서 막혀요.** App Group이 진짜 entitlement라서.
  (우회 가능: 위젯이 App Group 대신 Supabase에서 직접 읽기. 오프라인 갱신은 포기)
- **7일 서명 만료는 사람이 처리 안 해도 됨.** SideStore가 폰에서 스스로 갱신.
  (AltStore는 컴퓨터가 같은 WiFi에 있어야 함) 계정 정지 위험 없음.
- **Critical Alert**(무음 뚫는 알림)은 애플 별도 승인 — 개인 앱은 사실상 불가.

---

## 4. 맥북에서 시작할 때 — 이 순서로

### 0단계. 맥락 잡기
`CLAUDE.md`(작업 방식·디자인 규칙)와 `NATIVE_PLAN.md`(전환 계획)를 먼저 읽으세요.

### 1단계. 환경 확인
```bash
node -v          # Node 있는지
xcodebuild -version   # Xcode 있는지 (없으면 App Store에서 설치, 용량 큼)
pod --version    # CocoaPods (없으면 brew install cocoapods)
```

### 2단계. Capacitor로 감싸기
지금 웹 코드(`index.html`/`app.js`/`style.css`)를 **한 줄도 고치지 않고** 그대로 씁니다.
디자인이 "비슷해지는" 게 아니라 **같은 파일이 그대로 도는** 거예요.

```bash
npm init -y
npm i @capacitor/core @capacitor/cli
npx cap init "복약 트래커" com.leedalnim.medtracker --web-dir .
npx cap add ios
```

`capacitor.config.json`의 `webDir`는 저장소 루트. 상대경로만 쓰고 있어서 그대로 동작해요.

### 3단계. ⭐ AlarmKit이 무료 계정에서 되는지 확인 ← 가장 먼저 할 것
여기서 갈립니다. 안 되면 뒤 계획이 다 바뀌어요.
- `ios/App/App/Info.plist`에 `NSAlarmKitUsageDescription` 추가
- 아주 작은 Swift 테스트로 알람 하나 예약해보기
- **무료 Apple ID로 서명 → 실기기 빌드 → 실제로 울리는지**

> Xcode에서 **Apple ID 로그인과 "이 개발자를 신뢰" 는 사람이 직접** 눌러야 해요.
> 그 뒤의 빌드/실행은 `xcodebuild`로 자동화 가능.

### 4단계. 그 다음
- 알람 예약/취소를 웹 코드와 연결 (Capacitor 플러그인)
- 기존 기록 이전: 새 앱은 저장소가 비어 있지만 **설정 > 클라우드 백업 > 복구 코드**만 넣으면 전부 들어옴
- 위젯은 유료 계정 결정 후

---

## 5. 이 환경(클라우드)과 맥북의 차이

지금까지 작업한 클라우드 환경에는 **Xcode가 없어요.** 그래서 Swift 코드를 써도 빌드 확인이 불가능했어요.
맥북에서는 `xcodebuild` 실행 → 오류 읽기 → 수정을 **직접 반복**할 수 있습니다.
웹 작업에서 Playwright로 화면을 확인하던 것과 같은 방식으로요.

또 하나: 이 환경은 `supabase.co` 접속이 **차단**돼 있어요(403). 맥북에서는 열려 있을 테니
서버 상태를 직접 확인할 수 있어요.

---

## 6. 아직 안 한 작은 것들

- [ ] Edge Function `med-push`에 `urgency: 'high'` 반영 (저장소엔 이미 커밋됨, **대시보드 재배포만 필요**)
      — 기본값 `normal`이면 애플이 배터리 아끼려고 모아뒀다 늦게 배달해요. 실제로 겪었음.
- [ ] 고양이 복약 트래킹 (두 번째 프로필) — 계획만 있고 미룸
- [ ] 재알림 ("21시에 알렸는데 30분 지나도 기록 없으면 한 번 더") — 아이디어만
