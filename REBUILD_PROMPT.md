# 복약 트래커 재구축 프롬프트 (DB 연결 · 실제 약 데이터 버전)

> 아래 내용을 그대로 붙여넣어 사용하세요. 우리가 만든 웹앱(localStorage 전용, 정적 파일)을 **DB 백엔드 + 실제 약 데이터 조회**로 업그레이드해 재현하는 지시입니다. 이미지 첨부 불가 환경이므로 **모든 아이콘은 이모지/유니코드/CSS로만** 처리하세요.

---

## 지시 (여기부터 복사해서 사용)

한국어 **복약 트래커** 웹앱을 만들어줘. 복용 간격과 하루 최대치를 기준으로 "다음 복용 가능 시각"을 계산해 알려주고, 달력에서 생리주기도 기록·예측하는 앱이야. 모바일 우선(세로 화면), 다크/라이트 지원. **데이터는 DB에 저장**하고, **약 이름은 실제 의약품 데이터에서 검색**할 수 있게 해줘.

### 0. 제약/원칙 (반드시 지킬 것)
- **이미지 파일 사용 금지** — 앱 아이콘·삽화 포함 모든 아이콘은 **이모지 또는 인라인 SVG(코드로 그리는 것)나 CSS**로만. 외부 이미지 첨부/업로드 없음.
- **푸시 알림 없음** (계산·기록만).
- **의료 판단 없음** — 앱은 사용자가 입력/조회한 간격·최대치·날짜로 **산술 계산만** 함. 화면에 "이 앱은 계산 도구이며 복약 지도는 의사·약사와 상담하세요" 취지의 안내 문구 포함.
- 폰트는 **Pretendard** (없으면 system-ui/Noto Sans KR 폴백).

### 1. 핵심 계산 로직 (가장 중요 — 정확히 구현)
약(med)은 두 가지 **기록 방식(type)**이 있어:
- **interval(간격 트래커)**: 다음 복용 가능 시각 계산. 필드 = `interval_hours`(최소 간격 시간, 소수 허용), `max_per_day`(1일 최대 개수).
- **check(복용 체크)**: 간격 계산 없이 "오늘 먹었는지"만 기록. `interval_hours` 없음, `max_per_day`는 선택.

각 복용 기록(dose)은 `{med_id, taken_at(타임스탬프)}`.

**계산 규칙(interval 약):**
- `마지막복용 = 그 약의 가장 최근 dose`.
- `다음복용가능시각 = 마지막복용.taken_at + interval_hours`.
- 지금이 그 이후면 **"지금 복용 가능"**, 이전이면 **남은 시간 카운트다운**(H:MM:SS, 매초 감소) 표시.
- **오늘 복용 횟수 = taken_at의 날짜가 오늘인 dose 개수**. `max_per_day`에 도달하면 "오늘 최대" 상태.
- **자정 처리(중요)**: "오늘 복용 횟수"는 자정에 0으로 리셋되지만(날짜 기준), **다음 복용까지 카운트다운은 실제 마지막복용 시각 기준으로 계속 진행**돼야 함. 즉 밤 11시에 먹고 간격 4시간이면 자정 넘어 새벽 3시에 복용 가능으로 떠야 함(자정에 리셋되면 안 됨).
- **경과 표시 초기화**: "마지막 복용 후 N시간 지남" 문구는 마지막 복용 후 **24시간이 지나면 숨기고** 깔끔하게 "지금 복용 가능"만 표시.

### 2. DB 데이터 모델
(사용자 계정이 있으면 각 테이블에 user_id. 단일 사용자면 생략 가능)
- **meds**: `id, name, unit(정/캡슐/포/회), type('interval'|'check'), interval_hours(nullable), max_per_day(nullable), created_at`
- **doses**: `id, med_id(FK), taken_at(timestamp)`
- **period_days**: `id, day(date)` — 생리로 표시한 날짜들(하루 1행). 연속된 날짜는 한 "회차(episode)"로 묶어 계산.
- **settings**: `theme('system'|'light'|'dark'), period_enabled(bool)` 등 키-값.
- (선택) **drugs**: 약 이름 검색용 시드 테이블 — 아래 4 참고.

### 3. 화면 구성 (하단 탭: 홈 / 달력 / 설정)
**홈**
- 등록된 약이 없으면 빈 상태 + "약 추가" 버튼.
- 약마다 **카드**: 왼쪽에 **원형 링**(interval 약은 남은 시간 카운트다운 또는 "지금 가능"; check 약은 링 없이 상태), 약 이름, "오늘 N/최대" 배지, 상태 문구, **"먹었어요"** 버튼(1회 복용 기록).
  - interval 대기중 상태 문구: **"N시 N분 이후 복용 가능"**(빨간 강조, 초는 표시 안 함).
  - "지금 가능" 상태: "마지막 복용 후 N시간 지남"(24시간 지나면 숨김).
- 카드를 왼쪽으로 밀면 삭제 버튼(스와이프 삭제).

**약 상세** (카드 탭)
- 큰 원형 히어로 링: 대기중이면 카운트다운(H:MM:SS)+"N시 N분 예정", 복용가능이면 "지금 복용 가능".
- 통계 타일: 오늘 복용 / 1일 최대 / 최소 간격.
- **복용 내역(최근 30일)**: 날짜별 그룹, 각 기록은 "N시 N분"(초 없음). 행을 밀면 수정/삭제.
- 하단: "복용 정보 수정" / "약 삭제" 버튼.

**약 추가 / 수정 폼**
- **약 이름**: 콤보박스 — 입력하면 **실제 약 데이터에서 검색**(4번), 목록에서 고르면 자동 채움(간격·최대·단위·방식). 목록에 없으면 직접 입력 가능.
- **기록 방식(간격 트래커/복용 체크) 선택은 '추가'에서만 노출**. **수정 화면에는 없음**(수정은 약 정보 = 이름·간격·최대·단위만).
- interval이면 "최소 간격(시간)"+"1일 최대"+"단위", check면 간격칸 없이 "1일 최대(선택)"+"단위".

**달력**
- 월 달력. 생리 기능이 켜져 있으면 생리일/예정/가임기/배란일을 색으로 표시(범례 포함), 상단에 "다음 생리 예정일" 배너.
- 날짜를 누르면 하단 패널에 그날의 복용 기록(약 이름 + "N시 N분", 밀면 수정/삭제)과 "생리 기록/지우기" 토글.

**생리 주기** (달력에서 진입, 기본 꺼짐 — 설정에서 켬)
- 예측 카드: **평균 주기 / 평균 기간** 타일 + 한 박스에 **다음 예정일 / 배란 예정일 / 가임기**를 색 점으로 구분해 함께 표시(D-day 포함). 배란=다음예정일-14일, 가임기=배란-5일~배란+1일. "추정치이니 피임·임신 근거로 삼지 말라"는 안내.
- 기록 추가/수정: **시작일 + 기간(일)만 입력, 종료일 자동 계산**(진행 중이면 미래 종료일도 허용). 기간 기본값=과거 평균(없으면 5).
- 기록 목록: **박스당 한 회차**, 시작일(크게)+"N일간 · 주기 M일"(작게). 밀면 **수정·삭제** 버튼 함께 노출. 탭 아님, 스와이프로.

**설정**
- 내 약 관리(수정/삭제, 약 추가).
- 화면 테마(시스템/라이트/다크).
- 생리주기 기능 토글.
- **데이터 백업**: 내보내기(전체 데이터를 JSON 파일로 저장)/불러오기(파일 선택해 복원). DB 버전에서는 "내 계정 데이터 내보내기/가져오기"로 구현.

### 4. 실제 약 데이터 연동
**연결할 데이터(우선순위):**
1. **식품의약품안전처 의약품개요정보(e약은요) OpenAPI** ← 주력. 일반의약품(타이레놀·이지엔6·부루펜·탁센 등) 제품명 검색 + **효능·사용법·주의사항** 제공. (이 플랫폼에서 MCP로 연결 가능)
2. (선택) **건강보험심사평가원 약가목록조회** ← 전문(처방)의약품까지 이름 검색 확장용(로수바이브정·본비바·라바로브 등).

**연동 방식:**
- 약 이름 콤보 입력 시 위 API로 **제품명 자동완성**. 선택하면 이름/성분 채우고, 약 상세에 **효능·사용법·주의사항을 접이식으로 표시(참고용)**.
- ⚠️ **용법·용량은 자유 텍스트라 "최소 간격/1일 최대" 숫자로 자동 파싱 불가.** 따라서:
  - 이름·효능·사용법·주의는 API에서 가져와 **표시만** 하고,
  - **최소 간격·1일 최대는 아래 시드 표를 기본값으로 자동 채우되 사용자가 수정 가능**하게. 시드에 없는 약은 사용자가 직접 입력.
- 공공데이터포털 API 키 필요(무료 발급). e약은요는 MCP 커넥터로 붙이면 키 설정이 간단.
- 아래 시드(우리가 쓰던 값)를 `drugs` 테이블 또는 자동완성 기본값으로 넣어줘:

| 이름 | 단위 | 방식 | 최소간격(h) | 1일최대 |
|---|---|---|---|---|
| 타이레놀정 500mg | 정 | interval | 4 | 8 |
| 타이레놀 8시간 이알 서방정 650mg | 정 | interval | 8 | 6 |
| 이지엔6프로 (덱시부프로펜 300mg) | 캡슐 | interval | 6 | 4 |
| 부루펜정 400mg (이부프로펜) | 정 | interval | 4 | 3 |
| 탁센 (나프록센 250mg) | 캡슐 | interval | 6 | 5 |
| 부스코판당의정 10mg | 정 | interval | 4 | 10 |
| 로수바이브정 | 정 | check | - | 1 |
| 본비바정 150mg (월 1회) | 정 | check | - | 1 |
| 라바로브정 | 정 | check | - | 1 |
| 라바로하이정 | 정 | check | - | 1 |

### 5. 디자인
- 액센트: **저채도 코랄 레드 `#D65B54`** (다크 모드 `#E68B84`). 배경 라이트 `#F4F4F5`/카드 흰색, 다크 `#0E0E10`/카드 `#1B1B1F`.
- 카드 라운드 22px, 부드러운 그림자, 넉넉한 여백. 하단 탭바는 떠 있는 알약형(반투명 유리 느낌이면 좋음).
- 시각 포맷: 카운트다운은 H:MM:SS, 기록/예정 시각은 **"N시 N분"**(초 생략), 날짜는 "M월 D일 (요일)".
- 화면 빈 공간 더블탭 확대 방지(`touch-action: manipulation`).

### 6. 동작 디테일
- "먹었어요" 누르면 현재 시각으로 dose 1건 추가.
- 복용 내역·생리 기록은 수정(시각/날짜)·삭제 가능.
- 생리 기능 끄면 달력·생리 관련 UI 모두 숨김.
- 데이터 손실 방지를 위해 백업(내보내기/불러오기) 반드시 포함.

(여기까지 복사)

---

## 참고: 원본과의 차이 (플랫폼 특성 반영)
- **저장**: 원본 localStorage → **DB 테이블**.
- **약 목록**: 원본은 하드코딩 10종 → **실제 의약품 API 검색 + 시드 기본값**.
- **아이콘**: 원본은 생성 PNG → **이모지/인라인 SVG/CSS** (이미지 첨부 불가 대응).
- 나머지 화면·로직·문구는 원본 그대로 재현하도록 지시에 명시함.
