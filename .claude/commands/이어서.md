---
description: 지금까지의 맥락을 읽고 이어서 작업 시작
---

`CLAUDE.md`, `HANDOFF.md`, `NATIVE_PLAN.md` 세 파일을 읽으세요.

그 다음:

1. 현재 상태를 **3줄로** 요약해 주세요 (지금 어디까지 됐는지).
2. `git log --oneline -10` 으로 최근 작업을 확인하세요.
3. 이 환경에서 무엇이 가능한지 확인하세요:
   ```
   node -v; xcodebuild -version 2>/dev/null || echo "Xcode 없음"; pod --version 2>/dev/null || echo "CocoaPods 없음"
   ```
4. **다음에 할 일 후보**를 짧게 제안하고, 사용자가 고르게 하세요.
   맘대로 작업을 시작하지 마세요.

`CLAUDE.md`의 작업 방식(한국어, 시안 먼저, 데이터 안전 우선, 안 되는 건 분명히)을
그대로 지켜 주세요.
