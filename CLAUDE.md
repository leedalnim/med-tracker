# 복약 트래커 — 작업 안내

이 파일은 **다음에 이 저장소에서 작업할 Claude를 위한 안내**예요.
새 세션에서도 지금까지와 같은 방식·같은 디자인으로 이어가기 위한 것입니다.

---

## 0. 사용자와 일하는 방식 (중요)

- **한국어로 대화**해요. 존댓말, 담백하게.
- **디자인 변경은 시안 먼저.** "일단 변경하지 말고 시안만" 이라는 요청이 여러 번 있었어요.
  HTML 목업을 만들어 Playwright로 캡처해 보여주고, 고르신 뒤에 적용해요.
  안이 여러 개면 각각의 장단점을 솔직히 쓰고 **추천 하나를 고릅니다.**
- **기존 데이터는 절대 건드리지 않아요.** 가장 자주, 가장 강하게 요구하신 원칙이에요.
  구조를 바꿀 땐 "왜 안전한지"를 먼저 설명하고, 테스트로 증명해요.
- **작업 후엔 실제 화면을 캡처해서** 보여드려요 (시안 말고 진짜 렌더 결과).
- 안 되는 건 **안 된다고 분명히** 말해요. 되는 것처럼 얼버무리지 않아요.
  (예: 웹 푸시는 알람이 될 수 없다 — 이걸 늦게 말해서 헛걸음한 적이 있어요)
- 확실하지 않으면 **검색해서 원문까지 확인**해요. 블로그 요약이 틀린 경우가 실제로 있었어요.

---

## 1. 프로젝트 개요

- **바닐라 JS PWA.** 프레임워크·빌드 도구 없음. `app.js` 하나에 전부 들어 있어요 (IIFE).
- **GitHub Pages 배포.** `main`에 push하면 바로 반영돼요.
- 저장은 **localStorage가 원본**, Supabase는 사본(클라우드 백업).
- 사용 환경은 **아이폰 홈 화면에 추가한 PWA** (iOS 26).

```
index.html    화면 뼈대 (거의 안 건드림)
app.js        전부 — 상태, 계산, 렌더, 이벤트
style.css     디자인 토큰 + 모든 스타일
sw.js         서비스워커 (오프라인 캐시 + 푸시 수신)
manifest.json PWA 설정
supabase/     서버 쪽 (SQL, Edge Function) — 사용자가 대시보드에 직접 붙여넣음
```

### 배포 절차 (매번 지킬 것)

1. `app.js`의 `APP_VERSION` 과 `sw.js`의 `CACHE_NAME` **둘 다** 올려요. 하나만 올리면 폰에 반영이 안 돼요.
2. 커밋 → `claude/med-tracker-pwa-tysuvp` 브랜치에 push → `main`에 fast-forward 머지 후 push.
3. 사용자에게 **"설정 맨 아래에서 버전 확인"** 을 안내해요.
   PWA가 옛 버전을 붙잡고 있어서 "고쳤는데 그대로"인 일이 실제로 있었어요.
   설정 맨 아래 `버전 · 업데이트 확인` 버튼을 누르면 강제로 확인합니다.

---

## 2. 디자인 원칙

색·간격은 **`style.css` 상단 토큰**을 쓰고, 새 색을 함부로 만들지 않아요.

```
--bg #F4F4F5   --card #FFFFFF   --text #111114   --text-dim #86868E
--line #ECECEF  --accent #D65B54 (코랄)  --accent-soft (코랄 10%)
--radius 22px   --radius-sm 14px
```

- 다크 토큰은 **세 곳**에 있어요 (`[data-theme=dark]`, `.dark-tokens`, `prefers-color-scheme`).
  색을 추가하면 **세 곳 다** 넣어야 해요.
- 폰트는 Pretendard Variable. 굵기는 700/800을 주로 써요.
- **카드 + 둥근 모서리 + 옅은 그림자**가 기본 언어예요. 테두리선은 거의 안 써요.
- 아이콘은 **lucide** 스타일, `lucide()` 헬퍼로 만들어요. `.lucide` 에 기본 크기가 없으니
  쓰는 곳마다 `width/height`를 지정해야 해요 (안 하면 안 보여요 — 실제로 겪었음).
- 강조는 코랄 하나로 충분해요. 여러 색을 쓰지 않아요.

### 사용자가 특히 신경 쓰는 것

- 여백·정렬이 조금만 어긋나도 바로 알아채세요. 구분선이 붙어 보이는 것, 소제목 위 여백 등.
- **버튼처럼 보이는데 안 눌리는 것**을 싫어해요. 잠긴 스위치는 눌렀을 때 이유를 띄워요.
- 글자가 많은 걸 싫어해요. 긴 설명은 제목 옆 **`?` 도움말 레이어**로 빼요 (`HELP` 객체).

---

## 3. 데이터 구조

```js
mt.meds      [{id, name, unit, type, intervalHours, maxPerDay, alarmMode?, alarmTime?}]  홈에 있는 약
mt.favorites 같은 모양 — 즐겨찾기(홈엔 안 뜸)
mt.doses     [{id, medId, ts}]   복용 기록
mt.period / mt.spotting  ['YYYY-MM-DD']  생리 / 부정출혈
mt.cloudKey  복구 코드(UUID) — 이걸 아는 기기만 백업 접근
mt.notifOn / mt.notifMeds / mt.pushSub   알림 (기기 전용, 백업 안 됨)
```

`type`은 `'interval'`(간격 트래커) 또는 `'check'`(복용 체크, 하루 1회).

### 절대 깨면 안 되는 것

- **약을 지워도 `mt.doses`는 남아요.** 그래서 약 id가 바뀌면 기록이 주인을 잃어요.
  '홈에서 내리기'는 삭제가 아니라 **id를 유지한 채 favorites로 이동**하는 방식이에요.
  다시 올리면 예전 기록이 이어져요. 이 동작을 깨지 마세요.
- `renderMedForm`은 저장할 때 med 객체를 **새로 만들어요.** 폼에 없는 필드
  (`alarmMode`, `alarmTime` 등)는 명시적으로 옮겨 담아야 사라지지 않아요.
- `migrate()`의 옛 보정 코드도 마찬가지 이유로 **필드를 통째로 복사한 뒤** 보정해요.

---

## 4. 클라우드 백업 · 알림

### 안전 원칙 (그대로 유지)

1. 저장은 항상 로컬 먼저. 업로드는 뒤에서 조용히, 실패해도 로컬은 그대로.
2. 새 기기는 백업 **꺼짐**으로 시작 — 빈 데이터가 클라우드를 덮어쓰는 사고 차단.
3. 복원은 사용자가 직접 누를 때만.
4. 기록이 하나도 없으면 자동 업로드하지 않음.

### 알림 구조

폰은 스스로 못 깨어나므로 **'다음 복용 시각'을 서버에 맡기고** 그 시각에 서버가 푸시를 보내요.

```
앱: 약/기록/알림설정 변경 → 4초 뒤 med_alarm_sync (예약 목록 통째로 교체)
서버: pg_cron 매분 → Edge Function med-push → 때가 된 예약 발송 → sent_at 기록
```

- 알림 시각은 약마다 **`간격 기준`**(마지막 복용 + 간격) 또는 **`매일 시각`**(고정) 중 선택.
- 테이블은 RLS 켜고 **정책 없음**. 복구 코드를 아는 경우에만 `SECURITY DEFINER` 함수로 접근.
- 저장소가 **공개**예요. VAPID **공개키만** 코드에 두고, 비밀키·크론 시크릿은 Supabase Secrets에만.

---

## 5. 테스트

Playwright로 실제 화면을 조작해서 확인해요. Supabase는 이 환경에서 접속이 막혀 있어
(`supabase.co` 403) **RPC를 route로 가로채서** 검증합니다.

```
chromium: /opt/pw-browsers/chromium-1194/chrome-linux/chrome
playwright: createRequire('/opt/node22/lib/node_modules/playwright/')
viewport 390x844, 로컬 정적 서버로 저장소를 서빙
시각에 의존하는 테스트는 page.clock.setFixedTime() 으로 고정 (안 그러면 시간대별로 깨져요)
```

무엇을 바꾸든 **기록이 그대로인지**를 마지막에 확인해요.

---

## 6. 현재 상태 (2026-09 기준)

- 웹 PWA로 할 수 있는 건 대체로 끝났어요. 알림까지 붙어서 동작 중.
- 남은 아쉬움은 **알람과 위젯** — 웹으로는 불가능해요.
  자세한 건 `NATIVE_PLAN.md` 참고. 네이티브로 갈 때 그 문서부터 읽으세요.
- 미룬 것: 고양이 복약 트래킹(두 번째 프로필)
